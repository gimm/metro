'use strict';

describe('Serivce: grid', function () {

    // load the directive's module
    beforeEach(module('metroApp'));

    var element,
        scope;

    beforeEach(inject(function ($rootScope) {
        scope = $rootScope.$new();
    }));

    iit('should add group to ui', inject(function (grid, $document) {

    }));
});
